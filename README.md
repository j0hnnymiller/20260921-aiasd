# Todo List Manager

A lightweight browser-based todo manager with SQLite persistence, filters, due dates, priorities, editing, and bulk actions.

## Run

Install dependencies and start the local server:

```powershell
npm install
npm start
```

Then open http://localhost:3000 in a browser.

The SQLite database is created automatically at `data/tasks.sqlite`.
